{
  "name": "tylerrosnett-com",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "deploy:preview": "astro build && wrangler versions upload",
    "astro": "astro",
    "perf:serve": "astro preview --host 127.0.0.1 --port 4321",
    "perf": "node scripts/perf.mjs",
    "perf:assets": "node scripts/perf-assets.mjs"
  },
  "dependencies": {
    "astro": "^6.2.2"
  },
  "devDependencies": {
    "@catppuccin/tailwindcss": "^1.0.0",
    "@tailwindcss/vite": "^4.2.4",
    "chrome-launcher": "^1.2.1",
    "lighthouse": "^13.2.0",
    "tailwindcss": "^4.2.4",
    "wrangler": "^4.0.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
